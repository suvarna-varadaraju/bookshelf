<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.android.bookshelf.ui.home.HomeFragment"
        android:label="@string/menu_launchpad"
        tools:layout="@layout/fragment_home"/>

    <activity
        android:id="@+id/nav_description"
        android:name="com.android.bookshelf.ui.home.FavouritesActivity"
        android:label="@string/menu_description"
        tools:layout="@layout/activity_favourites" />
</navigation>